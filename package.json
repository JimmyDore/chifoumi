{
  "name": "chifoumi",
  "version": "1.0.0",
  "description": "Pierre, feuille, ciseaux à distance — pour trancher les petites décisions du quotidien.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "test": "node --test server/test/*.test.mjs"
  },
  "dependencies": {
    "@fastify/static": "^10.1.3",
    "@fastify/websocket": "^11.0.0",
    "fastify": "^5.0.0"
  }
}
